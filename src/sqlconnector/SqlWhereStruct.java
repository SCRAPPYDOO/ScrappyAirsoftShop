/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package sqlconnector;

/**
 *
 * @author User
 */
public enum SqlWhereStruct {
    WHERE_COLUMN,
    WHERE_OPERATOR,
    WHERE_FILTER,    
    WHERE_SEPARATOR,
}
